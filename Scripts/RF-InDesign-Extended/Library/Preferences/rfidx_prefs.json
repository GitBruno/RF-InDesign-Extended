// Comments are allowed, but only on their own line, as this one is.
{
  // This is the name of the directory inside the InDesign scripts folder.
  "userFolderName"        : "RF-InDesign-Extended",
  // This is the name of the directory you want to use to create your menu hierarchy.
  "userMenuFolderName"    : "Menus",
  // This is the name of the directory to look in to create event handlers from.
  "userEventsFolderName"  : "Event Handlers",
}
