#targetengine "main"var file = new File("~/Desktop/missing_links.txt");file.open("w");file.encoding= 'UTF-8';var theText = file.write(main().join("\n"));file.close();file.execute();function main () {  var result = ["Missing links:\n=============="];  for (var i = app.documents.length - 1; i >= 0; i--) {    var doc = app.documents[i];    result.push("\n");    result.push("-------  "+doc.name+"  -------");    for (var j = doc.links.length - 1; j >= 0; j--) {      if (doc.links[j].status == LinkStatus.LINK_MISSING || doc.links[0].status == LinkStatus.LINK_INACCESSIBLE) {        result.push(doc.links[j].filePath)      }    }  }  return result;}